# Copy to ./secrets/s3_credentials and fill in your Hetzner Object Storage keys.
# This is an AWS shared-credentials file; the SDK reads the [default] profile.
# The file is mounted into the uploader container at /run/secrets/s3_credentials.
[default]
aws_access_key_id = your-access-key
aws_secret_access_key = your-secret-key
